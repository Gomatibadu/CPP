#include<stdio.h>
 int main()
 {
     int b;
    printf("\n enter a non zero number;");
    scanf("%d",&b);
    if(b%2==0)
    printf("\n the no %d is even,"b);
    else
    printf("\n the no %d is odd",b);
      return 0;

    
 }
